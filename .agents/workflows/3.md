---
description: 3. 更新內部邏輯與系統指令
---

1. 開場詢問使用者這次想要調整專案的哪一個部分？（選項可包含：`A. 修改 AI 分析師人設 (NotebookLM Prompt)`、`B. 新增/調整抓取的新聞關鍵字`、`C. 修改抓蟲邏輯`）。
2. 使用者回答後，利用 `view_file` 或 `grep_search` 深入 `modules/` 裡面的 `data_fetcher.py`、`analyzer.py` 或 `notebooklm_generator.py` 等腳本找出對應程式碼。
3. 提出修改建議方案。確認後，自動使用 `multi_replace_file_content` 更新對應腳本。
4. 提醒使用者可以後續輸入 `/1` 來重新執行測試，確認新邏輯是否正確運作。
